import ClientPlaceholder from "../ClientPlaceholder";

export default function BookingRequestsPage() {
  return (
    <ClientPlaceholder
      title="Booking Requests"
      description="Track open requests waiting for confirmation from test centers."
    />
  );
}
