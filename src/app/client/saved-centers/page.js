import ClientPlaceholder from "../ClientPlaceholder";

export default function SavedCentersPage() {
  return (
    <ClientPlaceholder
      title="Saved Centers"
      description="Centers you have shortlisted for upcoming exams and events."
    />
  );
}
