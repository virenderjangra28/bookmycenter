import Enterprise from "./Enterprise";

export const metadata = {
  title: "Enterprise | BookMyCenter",
  description:
    "Upload one exam requirement and match verified centers across multiple cities from a single command center.",
};

export default function EnterprisePage() {
  return <Enterprise />;
}
